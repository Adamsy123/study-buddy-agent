[project]
name = "study-buddy-agent"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Adamsy123", email = "Adammohammed5000@gmail.com" }
]
requires-python = ">=3.14"
dependencies = [
    "openai>=3.16.2",
    "python-dotenv>=1.2.3",
    "requests>=2.34.2",
    "streamlit>=1.64.0",
]

[project.scripts]
study-buddy-agent = "study_buddy_agent:main"

[build-system]
requires = ["uv_build>=0.12.1,<0.13.0"]
build-backend = "uv_build"
